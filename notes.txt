"background (use this later for the recognition script which makes a picture with the webcam every seconds or something)" : {
    "scripts": ["background.js"]
},

"browser_action (this adds an icon to the navbar. Use this to make reference to the options page.": {
}

"permissions": [
    "storage",
    "tabs",
    "activeTab"
]

"content_security_policy": "script-src 'self' 'unsafe-eval'; object-src 'self';",

"storage": {
  "managed_schema": "schema.json"
}